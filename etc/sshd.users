# /etc/firejail/sshd.users - restricted user shell configuration
#
# Each user entry consists of a user name, semicolon, firejail
# program arguments, for example:
#
#netblue:--debug --net=none
#
# The extra arguments are inserted into program command line if firejail
# was started by SSH server.

